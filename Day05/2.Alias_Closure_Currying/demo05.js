// function hrToMin(value) {
//     return 60 * value
// }
const hrToMin = (value) => { return 60 * value }

function kmTom(value) {
    return 1000 * value
}

function feetTocm(value) {
    return 30 * value
}

// const res = hrToMin(2)
console.log('hr to min - ' + hrToMin(2))
console.log('kmto m - ' + kmTom(2))
// console.log('feet to cm - ' + feetTocm(2))